let readlineSync = require('readline-sync')


let numero = readlineSync.question('Informe seu numero: ')

for(let i = 1; i<=numero; i++) {
  let soma = nume
}

console.log(`Soma dos ${numero} primeiros numeros inteiros são: ${soma}`)